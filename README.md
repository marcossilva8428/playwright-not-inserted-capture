# playwright-not-inserted-capture
Script em Python usando Playwright que automatiza login no sistema VSN, navega até contratos comerciais, abre playlist de anúncios e executa importação. Em seguida, localiza a tabela “Not Inserted”, soma os valores exibidos via JavaScript e imprime o total de itens não inseridos no console.
